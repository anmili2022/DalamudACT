# DPS统计 更新记录

> 编码说明：本文件已于 `2026-05-24` 重新整理为 UTF-8 可读版本。旧内容中已有多处中文损坏，无法可靠恢复，以下内容保留当前仍有维护价值的版本线索与最新变更摘要。

## Unreleased

## 0.15.2.71 - 2026-07-14

### 队友监控

- 修复同职业双暗骑场景中，一个暗骑释放至黑之夜或献奉时，另一个暗骑也错误进入冷却的问题。
- 非战斗暂停时，点击技能监控标题右侧的“暂停”徽标会立即刷新一次队友技能和食物缓存。

### 状态监控

- 非战斗暂停时，点击状态监控标题右侧的“暂停”徽标会立即刷新一次自身和目标状态缓存。

### DPS 悬浮窗

- 极简模式总 DPS 栏增加悬停提示，显示 `[战斗时间-总DPS]`。
- 队友 DPS tooltip 增加死亡次数显示。

### 时间轴

- 副本内保留轻量的结算ID和读条ID应对方案 TTS 触发，不恢复重型自动同步和跳轴。
- 读条ID应对方案只在当前时间轴存在读条应对配置时低频扫描，并且只处理白名单 actionId，降低卡顿风险。

### 维护

- 发布流程文档补充本机构建无法解析 Dalamud 引用时的 `DalamudLibPath` 兜底构建命令。

## 0.15.2.70 - 2026-06-14

### 战斗流水

- 副本内不再自动强制使用轻量战斗流水，改为按用户设置决定是否降载。
- 未开启高性能模式且未手动开启轻量战斗流水时，副本内会继续记录伤害、治疗、读条、状态等完整流水，窗口勾选项负责筛选显示。
- 高性能模式和手动轻量战斗流水仍保留原有降载行为，用于高压场景临时降低记录开销。

## 0.15.2.69 - 2026-06-11

### 性能策略

- 野外与特殊区域改为和主城/住宅区相同的低负载刷新策略，降低大型特殊地图中的默认后台压力。
- 时间轴模块现在只在副本区域启用；野外、主城和住宅区不会加载、显示或同步时间轴。
- 非副本区域会主动清理当前时间轴状态，避免旧时间轴残留。
- 保留 0.15.2.68 的副本内高频 Ability / StartsUsing 重同步保护，避免高压副本卡顿回归。

### 界面性能

- 时间轴悬浮窗增加行数据短缓存，减少每帧文本测量、截断和列表分配。
- DPS 面板增加战斗快照级排序/汇总缓存，减少每帧 LINQ 排序和数字文本解析。

## 0.15.2.68 - 2026-06-11

### 紧急性能修复

- 针对用户反馈“绝妖星中更新后帧率掉到个位数，卸载插件恢复”的情况，降低副本内默认后台负载。
- 副本内自动刷新策略不再使用低延迟预设，统计和时间轴刷新回到标准间隔。
- 副本内默认跳过时间轴 ActionEffect / StartsUsing 重型同步路径，避免高频技能包反复触发时间轴全表扫描；非副本且未开启高性能模式时仍保留机制同步能力。
- 时间轴定义加载后建立运行时索引，用 actionId 与事件类型索引定位 Ability、StartsUsing、响应 TTS、系统日志、喊话和场地特效同步项，减少同步、TTS 和可见条目刷新中的重复 LINQ 扫描与分配。
- 时间轴可见列表增加短缓存，降低时间轴窗口每帧刷新造成的排序和列表分配压力。
- 副本内战斗流水采用轻量口径，保留开战、结束、死亡、场地特效、头顶标记和连线等关键事件，跳过高频伤害、治疗和状态流水写入。
- 状态监控缓存状态名称和图标，避免刷新时重复查询 Excel sheet。
- 队友技能监控窗口不再每帧重建成员和技能显示列表，改为复用缓存状态并仅动态计算倒计时。
- 强化日志关闭时不再运行 ActionEffect、Framework、统计和 DoT 的性能分段计时，减少默认使用路径上的 Stopwatch 与列表分配开销。
- Framework 中副本内战斗对象轮询间隔上调，作为高压场景下的额外保护。

### 稳定性

- 回调、命令和 Hook 在插件释放过程中会提前返回，降低卸载或热重载时继续处理事件的风险。
- 状态监控悬浮窗默认禁用滚动条，减少窄窗口或折叠状态下的额外布局干扰。

## 0.15.2.67 - 2026-06-10

### 简易设置

- 简易设置新增独立“性能”页签，集中显示高性能模式、轻量战斗流水和 DoT/野火归属开关，避免基础页无滚动时控件被裁切。

### 队友监控

- 非战斗暂停刷新时，已缓存技能冷却和起效时间继续动态倒计时，黄色起效框会自然显示到持续时间结束。
- 技能监控的暂停标记统一放到 `技能监控 (数量)` 标题行右侧，与状态监控标题行暂停标记保持一致。
- 从副本返回非副本区域时，技能监控会自动刷新一次缓存，随后继续保持非战斗低负载暂停策略。

### 状态监控

- 状态监控暂停提示改为标题行右侧小徽标，鼠标悬停时显示完整说明，减少非战斗时的文本占位。

## 0.15.2.66 - 2026-06-09

### 性能优化

- 新增高性能模式，面向绝境、战场和高特效场景，保留主要 DPS/HPS、承伤、死亡和队友技能监控，同时跳过高开销的友方 NPC 自动观察、DoT/野火归属、详细战斗流水和非队伍 ActionEffect 深解析。
- 新增轻量战斗流水模式，只保留战斗开始/结束、死亡、场地特效、头顶标记和连线，减少伤害/治疗/状态等高频流水带来的字符串构造和 UI 历史压力。
- 新增“统计 DoT/野火归属”独立开关，默认开启；关闭后不再记录 DoT 候选、野火采样或运行 DoT/野火轮询模拟。
- ActionEffect 热路径增加队伍 ActorId 短缓存，减少高频技能事件中对 PartyList/ObjectTable 的重复扫描。
- ActionEffect 目标解析改为延迟查表，只有治疗目标或需要纳入友方 NPC 时才反查 ObjectTable。
- 同一个 ActionEffect 包内友方来源补解析最多尝试一次，避免 Boss 多段 AoE 对每个目标重复解析来源。
- 未开启伤害统计 debug 时不再为每个技能包分配调试目标列表，技能名也改为需要写统计/流水/日志时再懒加载。
- 强化日志开启时新增 ActionEffect 慢包日志，用于定位高特效战斗中的单包处理耗时。

### 稳定性

- 修复 CN 运行环境下直接访问 `IClientState.LocalPlayer` 可能触发 `MissingMethodException` 的问题，改用项目内兼容的本地玩家身份解析入口。
- PvP/战场中跳过统计、技能监控、时间轴的高频战斗采集入口，避免多人技能事件导致主线程压力过高。

### 设置界面

- 简易设置基础页新增高性能模式、轻量战斗流水、DoT/野火归属开关，并修复高性能模式说明文字被裁切的问题。
- 完整设置的维护/性能区域新增同款性能开关和说明。

## 0.15.2.36 - 2026-05-25

### 队友监控

- 新增队友监控悬浮窗，支持食物、减伤和团辅技能监控。
- 支持匿名模式，悬浮窗可用职业名代替玩家名。
- 支持本地玩家兜底显示，跨服队伍或无队伍时也能显示自身监控。
- 支持按职业勾选技能和添加自定义技能。
- 支持 CD 中技能隐藏开关，默认隐藏长 CD 图标并在剩余 10 秒内重新显示。
- 支持团辅 / 减伤合并显示开关，默认分组显示。

### 队友监控样式

- 增加队友监控样式卡片，可调整图标大小、倒计时数字大小、背景颜色和起效增强强度。
- 起效中的技能使用金色高亮边框，减伤起效额外显示 `效` 标记。
- 名字 / 职业标签使用职业占比条配色作为背景，白色文字提升可读性。
- 修复技能全部进入长 CD 后下一行图标上移占位的问题。

### 战斗统计

- 修复纯治疗事件独立开启假战斗的问题，避免刚进本或战斗结束后的妖精治疗生成 1 秒战斗。
- 修正机工士 `武装解除` 冷却时间为 120 秒。

### 兼容与调试

- 食物监控明确识别 `StatusId = 48` 的 `进食` 状态，并兼容当前 Dalamud 状态类型差异。
- 设置页新增 `打印当前BUFF` 调试按钮，用于排查状态 ID、分类、剩余时间等字段。
- 新增队友监控交接文档 `md/2026-05-25-party-monitor-handoff.md`。

### 文档与维护

- 新增 `md/ENCODING-GUIDE.md`，记录 UTF-8 写作规范和 Windows PowerShell 下的安全读写方式。
- 新增 `tools/Check-TextEncoding.ps1`，用于检查当前 Git 变更 / 未跟踪文本文件是否存在无效 UTF-8 或疑似 mojibake。
- 新增 `.editorconfig`，统一文本文件使用 UTF-8、LF 换行和文件末尾换行。
- 整理 `HANDOVER.md`、`md/SESSION-HANDOFF.md`、`md/2026-05-24-npc-party-handoff.md` 为可读交接入口。
- 更新维护入口文档：
  - `md/MAINTAINER-HOME.md`
  - `md/MAINTAINER-FIRST-HOUR-CHECKLIST.md`
  - `md/MAINTAINER-INDEX.md`
  - `md/MAINTAINER-DOC-MAP.md`
- 将 `HANDOVER.md` 中早期已损坏编码的历史块迁出到 `md/HANDOVER-LEGACY-MOJIBAKE-ARCHIVE.md`。

## 0.15.2.34 - 2026-05-24

### NPC 队友识别

- 接入并整理 NPC 队友识别链路：
  - 本地玩家；
  - `PronounModule <1>~<8>`；
  - `AgentHUD.PartyMembers`；
  - Dalamud `PartyList`；
  - `BuddyList`；
  - `ObjectTable` 中可识别的友方 NPC。
- 增加 / 整理友方 NPC 名单逻辑：内置 NPC 名单、自定义 NPC 名单、名字规范化、当前队伍成员展示。
- `OwnerId` 不再一律等同宠物归属；只有明确的 `Pet / Buddy / RaceChocobo` 走 owner 归属，信赖 / 剧情 NPC 队友应独立统计。
- 不再单靠 `StatusFlags.Hostile` 判断 NPC 队友，避免信赖 / 幻体 / 任务 NPC 被误判。

### UI 与显示

- 设置页新增 / 整理 `NPC 队友识别名单`，用于核对玩家和 NPC 队友是否进入统计。
- 悬浮统计增加对象显示口径：
  - 智能：多人仅玩家，单人可含友方 NPC；
  - 玩家 + 友方 NPC；
  - 玩家 + 敌方 NPC。
- 统计 payload 中保留 `participantKind`：
  - `player`
  - `friendlyNpc`
  - `hostileNpc`
- NPC 行支持高亮和专用 badge：友方 NPC 显示 `友`，敌方 NPC 显示 `敌`。

### Debug 战斗记录

- 战斗流水和 debug 战斗记录默认关闭，避免普通使用时产生过多记录。
- debug 战斗记录 UI 继续使用 `Boss / 小怪` 与 `友方` 两组。
- 友方记录项支持：标记、技能、BUFF、debuff。

### 稳定性

- `ActorControl` Hook 仍因启动崩溃风险默认禁用。
- 避免使用 `PronounModule.ResolvePlaceholder(string, byte, byte)`，改走 `MemberFunctionPointers.ResolvePlaceholder(...)`，避免运行时 `MissingMethodException`。
- 修正部分攻击技能附带 `Heal` 效果时被错误写成“治疗 Boss”的口径，当前 HPS 只统计目标是已追踪友方对象的治疗。

## 0.15.2.13 - 2026-05-14

### 设置页与 Ikegami

- 压缩设置页布局，减少说明文字和控件之间的空白。
- 调整 Ikegami 专属设置区，使滑条、下拉框和说明更适合窄窗口。
- 修复设置区部分文本显示异常的问题。

### DoT / 对账

- 继续完善 DoT 对账流程。
- 强化插件统计与 ACT hostile-only DoT 总量之间的对比说明。

## 0.15.2.12 - 2026-05-14

### Minimal / 悬浮窗

- 增加 / 整理 `Classic / Ikegami / Minimal` 三种悬浮窗展示模式。
- Minimal 模式聚焦极简 DPS 表格展示。

### DoT / 离线工具

- 增加 `tools/DotReconcile` 离线 DoT 对账工具。
- 支持读取插件 `history-records.json` 和 ACT `Network_*.log`。
- 默认采用 hostile-only DoT 口径，将 ACT 已归属个人数与 ACT hostile 总量拆开。

## 0.15.2.11 - 2026-05-13

### DoT 与 owner 归属

- 继续修正 DoT 统计与 owner 归属逻辑。
- 减少队伍外对象 / 非目标对象导致的误归属。

## 0.15.2.10 - 2026-05-13

### Classic / Ikegami

- 整理 Classic 与 Ikegami 两类悬浮窗样式。
- 增加与调整 Ikegami overlay 的尺寸、透明度、tooltip 和 footer 表现。

## 0.15.2.9 - 2026-05-12

### DoT 统计

- 玩家 DoT 主链路切换为“状态驱动 + 3 秒轮询补 tick”，不再依赖事件包里的 DoT tick 直接记账。
- `PlayerDotCatalog` 作为静态白名单入口，按 `actionId / statusId` 过滤 DoT 候选，避免继续靠技能名字猜测。
- DoT 暴击改为沿用普通技能的暴击率模拟逻辑。
- DoT 活跃状态优先回归到白名单中的稳定技能信息，减少同一个持续伤害在日志里频繁切换 actionId。

### 概览与流水

- 概览页角色详情新增 `DoT总伤害`。
- 战斗流水窗口新增 `只显示技能` 筛选。
- 技能筛选新增搜索输入框。

## 0.15.2.8 - 2026-05-09

### 悬浮统计表格列

- 修复列宽持久化，使列宽按语义槽位保存，而不是按当前视觉顺序保存。
- 隐藏统计列时隐藏整列，而不是只隐藏单元格内容。
- 共享列会在固定列显示 / 隐藏后自动填满剩余宽度。
- 死亡列增加最小宽度保护。

### UI 与交互

- 设置窗口标题显示当前加载的插件程序集版本。
- 主窗口保留版本文本，便于测试时核对构建。
- 历史记录预览支持在配置时间后自动回到实时 DPS。
- 悬浮窗在战斗结束后保留上一场数据，下一场战斗真正开始时再清空。
- 状态文本区分“等待下一场战斗”和“正在收集新战斗数据”。

### 发布与维护

- 重新验证正式 tag 发布流程。
- 重新验证 latest 构建 / 发布自动化路径。

## 更早版本

更早版本的细节请参考：

- `HANDOVER.md`
- `md/SESSION-HANDOFF.md`
- `md/2026-05-09-RELEASE-HANDOFF.md`
- `md/2026-05-10.md`
- `md/2026-05-12-ikegami-handoff.md`
- `md/2026-05-13-*`
