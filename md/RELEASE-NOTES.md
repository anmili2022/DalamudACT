# DalamudACT {{VERSION}} 发布说明

- 当前发布版本：`{{VERSION}}`
- 这个文件会被 `.github/workflows/release.yml` 用作 GitHub Release 正文模板。
- 请保留 `{{VERSION}}` 占位符不变；正式发布时会自动替换成实际 tag 版本。

## 主要更新

### DoT 统计链路继续收口

- 玩家 DoT 主链路已切换为“状态驱动 + 3 秒轮询补 tick”，不再依赖事件包里的 DoT tick 直接记账。
- `DalamudACT/Stats/PlayerDotCatalog.cs` 已作为静态白名单入口，按 `actionId / statusId` 过滤 DoT 候选，避免继续靠技能名字猜测。
- DoT 暴击改为沿用普通技能的暴击率模拟逻辑。
- DoT 活跃状态现在会优先回归到白名单中的稳定技能信息，减少同一个持续伤害在日志里频繁切换 actionId 的情况。

### 概览页新增 DoT 总伤害

- 概览页角色详情现在会在“死亡”下方显示 `DoT总伤害`。
- 该值只累计插件已识别并记账的 DoT 伤害，包括真实归因到 DoT 的伤害与模拟补算的 DoT tick。

### 战斗流水筛选增强

- 战斗流水窗口新增 `只显示技能` 筛选。
- 现在可以叠加“角色 + 技能 + 事件类型 + 目标”筛选，只看某个角色的某个技能。
- 技能筛选新增搜索输入框，技能较多时可先输入关键字再选择目标技能。

## 说明

- 本次版本主要聚焦于 DoT 统计稳定性、概览信息补齐以及战斗流水可用性增强。
- 当前工作区已经完成本地构建验证，适合进入正式发布流程。

## 发布后检查

1. 确认 GitHub 已创建 `DalamudACT {{VERSION}}`。
2. 确认 Release 附件包含 `DalamudACT.zip`。
3. 确认 Release 正文中的版本号与主要更新正确。
4. 确认打包后的程序集版本、插件清单版本与 `repo.json` 中的版本一致。
