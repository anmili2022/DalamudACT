# 2026-05-11 工作记录

## 本轮背景

- 前面的战斗流水角色筛选、NPC/信赖/幻体显示与战斗结束判定问题已经基本收口；
- 本轮主要是在确认 DoT 的定义，避免继续把非 DoT 的技能误算进去，同时为下一会话准备交接上下文；
- 期间也复核了崩溃现场，确保后续实现不要再把游戏拖崩。

## 已确认的规则

1. 只统计真正会在目标身上形成持续伤害状态，并在状态存续期间持续结算的效果。
2. 目标进入无法选中状态后，DoT 后续结算应停止。
3. 不再用技能名字猜测 DoT。
4. 用户已明确：
   - `箭毒II` 不是 DoT；
   - `注药III` 不是 DoT。
5. 下一步改成静态表：
   - 21 个职业；
   - 每个职业 1~2 个 DoT 技能；
   - 以 `actionId` 为主键更稳；
   - 需要兜底时再补少量辅助信息，而不是反过来靠文本启发式做主判定。

## 代码侧当前状态

- `DalamudACT/Stats/LocalStatsService.cs`
  - `ObservePotentialPlayerDotApplication(...)`
  - `TryRecordPlayerDotDamage(...)`
  - `PollActivePlayerDots(...)`
  - `CapturePlayerDotStatusesForHostileTargetLocked(...)`
  - `TryCreateActivePlayerDotStateLocked(...)`
  - `TryResolvePlayerDotAttributionLocked(...)`
  - `IsPlayerDamageOverTimeStatus(...)`
- `DalamudACT/Plugin/ACT.cs`
  - `ReceiveAbilityEffect(...)`
  - `HandleAbility(...)`
- 原先那套基于状态描述文本的启发式识别，已经从主链路移除。
- 已新增静态 DoT 白名单：
  - `DalamudACT/Stats/PlayerDotCatalog.cs`
  - 只按 `actionId / statusId` 过滤玩家 DoT 候选
  - 非白名单技能不会再进入 DoT 归因链路
  - 当前主路径已经变成“静态表候选 + 目标状态确认”

## 崩溃与稳定性提醒

- 崩溃现场仍建议优先看：
  - `E:\git\crash\1\crash.log`
  - `E:\git\crash\1\dalamud.log`
  - `E:\git\crash\1\output.log`
- 之前的高风险点已经加了总 try/catch：
  - `ACT.cs` 的 `ReceiveAbilityEffect(...)`
  - `LocalStatsService` 的 DoT 轮询与归因入口
- 上一轮本地构建验证通过：
  - `dotnet build E:\git\DalamudACT\DalamudACT.sln`
  - 结果：`0 warnings / 0 errors`
- 本轮新增的 `PlayerDotCatalog.cs` 已再次构建通过。

## 下一步建议

1. 先把这版静态表补测一轮，重点看：
   - 玩家施加的真实 DoT 能否持续结算；
   - 目标不可选中后是否能正确停算；
   - `注药III`、`箭毒II` 这类非 DoT 是否不会再被误记。
2. 如果还有漏项，再继续补 `PlayerDotCatalog.cs`。
3. 如果这一轮稳定，再考虑是否需要继续补更细的 DoT / HoT 边界。

## 工作区提醒

- 当前工作区仍是脏的。
- `1.txt` 是未跟踪文件，不要误删。
- 如果后续要继续改代码，先确认不要碰到已有未提交的文档和临时文件。
## 2026-05-11 收工交接补充

- 本轮已把 DoT 主链路改为“状态驱动 + 3 秒轮询补 tick”，并通过本地构建验证：`dotnet build E:\git\DalamudACT\DalamudACT.sln`。
- 关键改动文件：
  - `DalamudACT/Stats/LocalStatsService.cs`
  - `DalamudACT/Plugin/ACT.cs`
  - `DalamudACT/Stats/PlayerDotCatalog.cs`
- 目前状态：
  - 已知 DoT 技能会先进入候选，再靠目标 debuff 确认；
  - 轮询时只要目标仍可选中，就会按 3 秒节奏继续补 tick；
  - 暴击按普通技能暴击率逻辑模拟；
  - 目标不可选中后停止后续 DoT 结算。
- 仍建议下次接手前先做两件事：
  - 进游戏验证几个典型 DoT 职业是否稳定；
  - 再核对一遍 `PlayerDotCatalog.cs` 是否还缺白名单。
- 工作区里还有很多既有改动和未跟踪文件，尤其是 `1.txt`，不要误删。